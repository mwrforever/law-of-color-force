// Minimal stub of Cocos `cc` for unit testing pure logic without the engine runtime.
// Only the exports actually imported by tested modules need to exist here.
export class Vec3 {
  constructor(public x = 0, public y = 0, public z = 0) {}
}
